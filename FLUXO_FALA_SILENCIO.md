# 🔎 Fluxo de detecção de fala/silêncio

                ┌──────────────────────────────┐
                │   Chega novo frame de áudio  │
                └───────────────┬──────────────┘
                                │
                                ▼
                  ┌──────────────────────────────┐
                  │ VAD disponível?              │
                  │ (senão usa volume % fallback)│
                  └───────────────┬──────────────┘
                                  │
             ┌────────────────────┴────────────────────┐
             │                                         │
             ▼                                         ▼
   ┌───────────────────────┐                 ┌───────────────────────────┐
   │ VAD/volume indica fala│                 │ VAD/volume indica silêncio│
   └───────────┬───────────┘                 └───────────┬───────────────┘
               │                                         │
               ▼                                         ▼
 ┌──────────────────────────────┐          ┌──────────────────────────────┐
 │ Estava em silêncio?          │          │ Tempo desde última fala ≥    │
 │                              │          │ silenceTimeout?              │
 └───────────┬──────────────────┘          └─────────────┬────────────────┘
             │                                           │
             ▼                                           ▼
   ┌───────────────────┐                       ┌──────────────────┐
   │ Inicia noiseStart │                       │ Marca inSilence  │
   │ (conta ruído)     │                       │ finalizeTriggered│
   └─────────┬─────────┘                       │ = true           │
             │                                 └─────────┬────────┘
             ▼                                           │
   ┌──────────────────────────────┐                      ▼
   │ noiseDuration ≥              │        ┌──────────────────────────────┐
   │ NOISE_IGNORE_THRESHOLD?      │        │ Chama sendDeepgramFinalize() │
   └───────────┬──────────────────┘        └──────────────────────────────┘
               │
               ▼
   ┌──────────────────────────────┐
   │ Sai do silêncio (fala real)  │
   │ inSilence = false            │
   │ finalizeTriggered = false    │
   └──────────────────────────────┘



---




# 🔎 Fluxo de detecção de fala/silêncio

```mermaid
flowchart TD
    A[Chega novo frame de áudio] --> B{VAD disponível?}
    B -->|Sim| C{Indica fala}
    B -->|Não| D{Indica silêncio}

    C --> E{Estava em silêncio?}
    E -->|Sim| F[Inicia noiseStart (conta ruído)]
    E -->|Não| G[Atualiza lastActive]

    F --> H{noiseDuration ≥ NOISE_IGNORE_THRESHOLD?}
    H -->|Sim| I[Sai do silêncio (fala real)\n inSilence=false\n finalizeTriggered=false]
    H -->|Não| F

    D --> J{Tempo desde última fala ≥ silenceTimeout?}
    J -->|Sim e !inSilence| K[Marca inSilence=true\n finalizeTriggered=true]
    K --> L[sendDeepgramFinalize(source)]
    J -->|Não| D
